spring:
  application:
    name: bureauCepTest
    
  datasource:
    platform: h2
    url: jdbc:h2:mem:testdb;INIT=CREATE SCHEMA IF NOT EXISTS SA;DB_CLOSE_ON_EXIT=FALSE;MODE=ORACLE
    username: sa
    password: 
    
server:
  servlet:
    context-path: /bureauCepTest
    